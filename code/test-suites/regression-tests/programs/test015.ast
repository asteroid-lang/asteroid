
load "io".
load "util".

try
    let (x,x) = (1,2).
    println x.
catch Exception(kind, s) do
    if not isnone(s @index("Non-linear")) do
        . -- pass
    else
        throw Error("FAIL: unexpected error "+s).
    end.
end.