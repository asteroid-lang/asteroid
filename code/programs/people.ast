load "io".
load "util".

structure Person with
    data name.
    data age.
    data gender.
    end

let people = [
    Person("George", 32, "M"),
    Person("Sophie", 46, "F"),
    Person("Oliver", 21, "M")
    ].

for Person(name:".*p.*",_,_) in people do
  print name.
end
