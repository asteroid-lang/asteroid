-- Factorial
-- An implementation of factorial with properly
-- constrained inputs using pattern matching
-- with type classes and conditional matching

load "io".
load "util".

function fact

    with (n:%integer) %if n > 0 do
        return 1.
    orwith (n:%integer) %if n < 0 do
        return 2.
    end


println ("The output is: " + fact (4)).

