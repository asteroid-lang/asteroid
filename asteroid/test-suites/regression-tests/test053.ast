
load system io.
load "util".
let p = -1.
let w = p - 1.
let q = [-1].
io @println (p,w,q).

-- NOTE: workaround, pattern matching does not work on negative constants.
assert(p == -1).
assert(w == -2).
assert(q == [-1]).

