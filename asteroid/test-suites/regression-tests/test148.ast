load system io.

let g = 1.
module test with
   let x  = 1.
   let a = [1 to 10].
end

io @println (test@x).
io @println (test@a@2).

let test@x = 2.
let test@a@2 = -10.

io @println (test@x).
io @println (test@a@2).

io @println g.

