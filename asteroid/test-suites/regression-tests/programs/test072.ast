
-- Factorial

load "io".
load "util".

function fact 
    with 0 do
        return 1
    with n do
        return n * fact (n-1).
    end 

println ("The factorial of 3 is: " + fact (3)).
assert (fact(3) == 6).
