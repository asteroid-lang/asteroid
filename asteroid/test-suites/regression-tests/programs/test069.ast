
load "util".
load system io.

structure S with
    data x.
    end 
    
let x = 'S(S(0)).
let y = 'S(S(x)).
let z = y.

io @println y.
io @println z.
io @println (eval (z)).
assert ((eval (z)) is S(S(S(S(0))))).
